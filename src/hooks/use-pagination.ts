// function usePagination(data, currentPage, postsPerPage) {
//   const startIndex = (currentPage - 1) * postsPerPage;

//   const paginatedData = data.slice(startIndex, startIndex + postsPerPage);

//   return paginatedData;
// }
